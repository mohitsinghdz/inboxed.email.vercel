---
import '../index.css';
import Navbar from '../components/Navbar';
import Footer from '../components/Footer.astro';
import DownloadPopup from '../components/DownloadPopup';

export interface Props {
  title: string;
  description: string;
  canonical?: string;
  type?: 'website' | 'article';
  ogTitle?: string;
  ogDescription?: string;
  ogImage?: string;
  schemas?: Record<string, unknown>[];
}

const {
  title,
  description,
  canonical = 'https://inboxed.email/',
  type = 'website',
  ogTitle,
  ogDescription,
  ogImage = 'https://inboxed.email/og-default.png',
  schemas = [],
} = Astro.props;

const resolvedOgTitle = ogTitle ?? title;
const resolvedOgDescription = ogDescription ?? description;

// Ensure canonical always ends with a trailing slash so it matches the actual
// URL Vercel serves from Astro's static output (page/index.html structure).
// This fixes "Alternate page with proper canonical tag" in Google Search Console.
const normalizedCanonical = canonical.endsWith('/') ? canonical : canonical + '/';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- Standard Metadata -->
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={normalizedCanonical} />

    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/favicon.png" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />

    <!-- Open Graph -->
    <meta property="og:type" content={type} />
    <meta property="og:site_name" content="Inboxed" />
    <meta property="og:title" content={resolvedOgTitle} />
    <meta property="og:description" content={resolvedOgDescription} />
    <meta property="og:url" content={normalizedCanonical} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={resolvedOgTitle} />
    <meta name="twitter:description" content={resolvedOgDescription} />
    <meta name="twitter:image" content={ogImage} />

    <!-- Structured Data -->
    {schemas.map((schema) => (
      <script type="application/ld+json" set:html={JSON.stringify(schema)} />
    ))}
  </head>
  <body>
    <div class="bg-background text-foreground min-h-screen selection:bg-black selection:text-white flex flex-col">
      <Navbar client:load />
      <main class="flex-grow">
        <slot />
      </main>
      <Footer />
      <DownloadPopup client:idle />
    </div>
  </body>
</html>
